import React from "react";
import SearchBar from "./SearchBar";

const Discovery = () => {
    return (
        <div className="flex-[2]">
            <SearchBar />
        </div>
    );
};

export default Discovery;
