import { useRouter } from "next/router";
import { routes } from "./constants";
import isUserURL from "./isUserURL";

function findPath() {}

export default function getCurrentPathTitle() {
    const router = useRouter();
}
